import type { APIRoute } from 'astro'; import { absoluteUrl } from '../config/domains'; import { getPublishedArticles } from '../lib/content';
export const GET: APIRoute = async () => {const articles=await getPublishedArticles();const pageCount=Math.ceil(articles.length / 6);const pageUrls=Array.from({length:Math.max(0,pageCount-1)},(_,index)=>`<url><loc>${absoluteUrl(`/blog/page/${index+2}`)}</loc><changefreq>monthly</changefreq><priority>0.6</priority></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${articles.map((article)=>`<url><loc>${absoluteUrl(`/blog/${article.slug}`)}</loc><lastmod>${article.updatedAt.slice(0,10)}</lastmod><changefreq>monthly</changefreq><priority>0.7</priority></url>`).join('')}${pageUrls}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}});};
