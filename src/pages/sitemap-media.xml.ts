import type { APIRoute } from 'astro'; import { absoluteUrl } from '../config/domains'; import { getPodcastEpisodes, getResources, getVideos } from '../lib/content';
export const GET: APIRoute = async () => {const [episodes,videos,resources]=await Promise.all([getPodcastEpisodes(),getVideos(),getResources()]);const paths=[...episodes.map((asset)=>`/podcast/${asset.slug}`),...videos.map((asset)=>`/videos/${asset.slug}`),...resources.map((asset)=>`/resources/${asset.slug}`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map((path)=>`<url><loc>${absoluteUrl(path)}</loc><changefreq>monthly</changefreq><priority>0.6</priority></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}});};
